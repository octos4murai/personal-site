---
layout: base.njk
title: Home
---

<h2>Blog Posts</h2>

<ul>
    {% for post in collections.post | reverse %}
        <li>
            <span datetime="{{ post.data.date | htmlDateString }}">{{ post.data.date | displayDateString }}</span> -
            <a href="{{ post.url | url }}">{{ post.data.title }}</a>
        </li>
    {% endfor %}
</ul>

<h2>Links</h2>

<ul>
    {% for link in links %}
        <li><a href="{{ link.url | url}}">{{ link.name }}</a></li>
    {% endfor %}
</ul>