<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>
        {%- if title %}{{ title }} | {% endif %}
        {{- metadata.title -}}
    </title>

    <meta name="description" content="{{ description or metadata.description }}">

    <link rel="preconnect" href="{{ 'https://fonts.gstatic.com' | url }}"> 
    <link rel="stylesheet" href="{{ 'https://fonts.googleapis.com/css2?family=Inconsolata:wght@400;700&family=Major+Mono+Display&family=Merriweather&display=swap' | url }}">

    <link rel="stylesheet" href="{{ '/css/prism-ghcolors.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/style.css' | url}}">
</head>
<body>
    <header>
        <h2 id="site-title" class="center-text no-margin no-padding"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h2>
        <p class="center-text no-margin no-padding">{{ metadata.description }}</p>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <p class="center-text no-margin no-padding">MIT License</p>
        <p class="center-text no-margin no-padding"><a href="{{ metadata.sourceUrl | url }}">/octos4murai</a></p>
    </footer>
</body>
</html>